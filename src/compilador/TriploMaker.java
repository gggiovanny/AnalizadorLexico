package compilador;

import analizador.lexico.Token;

import java.util.ArrayList;

public class TriploMaker {

    public boolean compilar(ArrayList<Token> tokens) {
        // Funcion que se llama desde el analizador y se le pasa la lista con todos los tokens

        return false;
    }
}
